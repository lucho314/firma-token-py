# Copiar como .env (o editar el que se crea solo en %LOCALAPPDATA%\FirmadorToken\.env)
# URL del backend al que se piden y suben los documentos:
BASE_URL=http://localhost:3000
# Puerto local donde escucha el servicio de firma:
PORT=8765
