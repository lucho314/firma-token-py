# Copiar como .env (o editar el que se crea solo en %LOCALAPPDATA%\FirmadorToken\.env)
# URL del portal/backend de donde se bajan los pendientes y se suben los firmados:
BASE_URL=https://portalt.ater.gob.ar
# Puerto LOCAL donde escucha el servicio de firma (el navegador le pega a 127.0.0.1:PORT):
PORT=8765
